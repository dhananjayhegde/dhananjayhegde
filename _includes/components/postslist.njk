<section class="layout__compact">
    {% for post in postslist %}
        <article class="post-list" {% if post.url == url %} data-current="current item" {% endif %}>
            <h3>
                <a href="{{ post.url | url }}">
                    {% if post.data.title %}
                        {{ post.data.title }}
                    {% else %}
                        Untitled
                    {% endif %}
                </a>
            </h3>
            <div class="metadata">
                <time datetime="{{ post.date | machineDate }}">{{ post.date | readableDate }}</time>
                {% set tags = post.data.tags %}
                <div class="tag-list">{% include "components/tag-list-inline.njk" %}</div>
            </div>
            {% if post.data.excerpt or post.data.page.excerpt %}
                <p>
                    <!-- {{ post.data.excerpt }} -->
                    {% excerpt post %}
                </p>
            {% endif %}
        </article>
        <hr>
        {% endfor %}
    </section>